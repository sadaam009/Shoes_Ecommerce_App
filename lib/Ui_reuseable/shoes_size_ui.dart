import 'package:flutter/material.dart';

class ShoesSizeUi extends StatelessWidget {
  const ShoesSizeUi({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [],
    );
  }
}
